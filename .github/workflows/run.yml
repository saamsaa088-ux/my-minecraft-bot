name: Run Bot 24/7

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */5 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Force Install Bedrock Protocol
        run: npm install bedrock-protocol@latest

      - name: Run Bot
        run: node bot.js
        timeout-minutes: 350
        
